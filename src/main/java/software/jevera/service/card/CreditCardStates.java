package main.java.software.jevera.service.card;

public enum CreditCardStates {
    PROCESSING, ACCEPTED, IN_USE, BANK_BLOCKED, OWNER_BLOCKED// обрабатывается, обработана(принята), используется, заблокирована(банком), заблокирована(пользователем)
}
